{
  "name": "match-constraints",
  "version": "0.0.4",
  "description": "Check an object match a given set of constraints",
  "author": "Matthieu Bacconnier <matthieu.bacconnier@papiel.fr>",
  "scripts": {
    "test": "NODE_ENV=test mocha -R spec test/",
    "coverage": "NODE_ENV=test istanbul cover node_modules/mocha/bin/_mocha -- -R spec test/"
  },
  "main": "./lib",
  "repository": {
    "type": "git",
    "url": "https://github.com/Papiel/match-constraints/"
  },
  "keywords": [
    "match",
    "constraints",
    "object",
    "validation"
  ],
  "dependencies": {},
  "devDependencies": {
    "mocha": "1.17.x",
    "should": "3.1.x"
  },
  "licence": "WTFPL",
  "engines": {
    "node": ">=0.10"
  }
}
